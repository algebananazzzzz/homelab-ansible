---
- name: Trust internal certificate authority on workstation
  hosts: local
  gather_facts: false
  become: true

  tasks:
    - name: Install the internal CA
      ansible.builtin.include_role:
        name: proxy/internal_ca
        tasks_from: trust
