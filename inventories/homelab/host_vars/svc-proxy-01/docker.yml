---
compose_projects:
  - name: consul
  - name: traefik
    certificate: true
    certificate_domains:
      - home.arpa
      - auth.home.arpa
      - "*.svc.home.arpa"
      - "*.ops.home.arpa"
    files:
      - dynamic.yml
  - name: cadvisor
