---
ansible_user: "{{ admin_user }}"
ansible_ssh_common_args: >-
  -o ProxyCommand="ssh -p {{ hostvars['host'].ansible_port }} {{ admin_user }}@{{ hostvars['host'].ansible_host }} nc %h %p"
tailscale_advertised_routes:
  - 10.10.10.0/24
  - 10.10.20.0/24
  - 10.10.30.0/24
